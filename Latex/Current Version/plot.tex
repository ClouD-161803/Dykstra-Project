\documentclass[tikz]{standalone}

\usepackage{pgfplots}
\pgfplotsset{compat=newest}
\usetikzlibrary{plotmarks}

% Uncomment if you want groupplots (nicer alignment)
\usepgfplotslibrary{groupplots}

\begin{document}
\begin{tikzpicture}
  % Top axis
  \begin{axis}[
    at={(0,0)},
    anchor=north west,
    width=10cm,
    height=4cm,
    xmin=-5, xmax=1,
    ymin=0.5, ymax=2,
    xlabel={$x$ coordinate},
    ylabel={$y$ coordinate},
    grid=both,
  ]
    \draw[fill=blue, fill opacity=0.1, draw=blue!20]
        (axis cs: -1,0.5) rectangle (axis cs: 1,1);

    \draw[thick, blue!30] (axis cs:-2,2) -- (axis cs:1,0.5);
    
    \draw[very thick, red!30, line join=round] (axis cs:-1,1.4) -- (axis cs:-0.8,1.4) -- (axis cs:-1,1) -- (axis cs:-1,1.4);
        
    \addplot[
        black,
        thin, densely dotted, line cap= round,
        mark=*, mark size = 0.5, line join=round,
    ] table[x=x, y=y, col sep=comma] {data_iterates.csv};

    \addplot[
    green,
    only marks,
    mark=asterisk,
    mark size=1.5,
    ] coordinates {(0, 1)};

  \end{axis}

  % Middle axis
  \begin{axis}[
    at={(0,-4.5cm)},
    anchor=north west,
    width=10cm,
    height=4cm,
    xmin=0, xmax=50,
    ymode=log,
    ylabel={Error},
    xlabel={Iteration},
    grid=both,
    legend entries={Original, Modified},
  ]
    \addplot[
        black,
        thick,
        mark=*, mark size= 1,
    ] table[x=iteration, y=error, col sep=comma] {data_dykstra_original.csv};
    \addplot[
        black,
        thick,
        mark=x, mark size= 2,
    ] table[x=iteration, y=error, col sep=comma] {data_dykstra_modified.csv};
  \end{axis}

  % Bottom axis
  \begin{axis}[
    at={(0,-9cm)},
    anchor=north west,
    width=10cm,
    height=4cm,
    xmin=0, xmax=50,
    ylabel={Halfspace activity},
    xlabel={Iteration},ytick={0,1},
    grid=both,
    legend entries={Original, Modified},
  ]
    \addplot[
        black,
        thick,
        mark=*, mark size= 1,
    ] table[x=iteration, y=halfspace, col sep=comma] {data_dykstra_original.csv};
    \addplot[
        black,
        thick,
        mark=x, mark size= 2,
    ] table[x=iteration, y=halfspace, col sep=comma] {data_dykstra_modified.csv};
  \end{axis}
\end{tikzpicture}
\end{document}